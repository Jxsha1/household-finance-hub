---
interface Props {
  className?: string;
}

const { className = '' } = Astro.props;
---

<div class={`bg-white/90 backdrop-blur-sm rounded-2xl border border-sky-100/60 p-5 shadow-sm shadow-slate-100/50 ${className}`}>
  <slot />
</div>
